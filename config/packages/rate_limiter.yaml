framework:
    rate_limiter:
        transfer_limiter:
            policy: sliding_window
            limit: '%env(int:RATE_LIMIT_TRANSFER)%'
            interval: '1 minute'
            cache_pool: rate_limiter_cache
